<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.swu.pigeon.dao.UserDAO">
    <insert id="save" parameterType="User" >
        insert into staff values (#{id},#{username},#{dept},
        #{password},#{sex},#{telNumber},#{email},#{icon},
        #{status},#{conpanyId},#{originId},#{registerTime})
    </insert>
    <select id="findByTel" parameterType="String" resultType="User">
        select *
        from staff where telNumber=#{telNumber}
    </select>

</mapper>